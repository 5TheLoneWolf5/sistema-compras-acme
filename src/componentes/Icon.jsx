import styled from "styled-components";

const IconElement = styled.img``;

const Icon = (props) => {
    
    return (
        <IconElement {...props} />
    );

};

/* Icon.defaultProps = {

}; */

export default Icon;